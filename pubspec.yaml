name: pManager
description: A package to handle media uploads.
version: 0.0.1
environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  photo_manager: ^1.3.7
  firebase_auth: ^3.3.3
  shared_preferences: ^2.0.6